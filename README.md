# Advance-Programming

git clone https://github.com/your-username/GymProject.git
cd GymProject

## 2. Create a C# Console Project (if starting fresh)
dotnet new console -o GymProject
cd GymProject
This creates a new console project in a folder called nameofFolder.�

## 3. Run the C# Project
dotnet run
